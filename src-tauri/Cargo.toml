[package]
name = "remark"
version = "1.0.0"
description = "Lightweight native Markdown editor with live preview and AI formatting"
authors = ["remark"]
edition = "2021"

[lib]
name = "remark_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["json", "stream"] }
tokio = { version = "1", features = ["full"] }
dirs = "6"
futures-util = "0.3"
